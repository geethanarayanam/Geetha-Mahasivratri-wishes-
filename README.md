GeethaMahasivratri wishes 🕉️
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Maha Shivaratri | Divine Blessings</title>
    <style>
        /* Professional Reset & Centering */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            height: 100vh;
            display: flex;
            justify-content: center; /* Horizontal centering */
            align-items: center;     /* Vertical centering */
            background: radial-gradient(circle at center, #1a1b4b 0%, #050510 100%);
            font-family: 'Georgia', serif;
            color: #d4af37; /* Gold tone for professional look */
            overflow: hidden;
        }

        /* Divine Glassmorphism Card */
        .card {
            position: relative;
            text-align: center;
            padding: 4rem 2rem;
            max-width: 500px;
            width: 90%;
            background: rgba(255, 255, 255, 0.03);
            border: 1px solid rgba(212, 175, 55, 0.2);
            border-radius: 24px;
            backdrop-filter: blur(12px);
            box-shadow: 0 25px 50px rgba(0, 0, 0, 0.5);
            animation: fadeIn 2s ease-out;
        }

        .om-symbol {
            font-size: 5rem;
            display: block;
            margin-bottom: 1rem;
            filter: drop-shadow(0 0 15px #d4af37);
            animation: pulse 4s ease-in-out infinite;
        }

        h1 {
            font-size: 2.2rem;
            font-weight: 300;
            letter-spacing: 4px;
            margin-bottom: 0.5rem;
            text-transform: uppercase;
        }

        .mantra {
            font-size: 1.4rem;
            color: #fff;
            letter-spacing: 2px;
            margin-bottom: 2rem;
            opacity: 0.9;
        }

        .divider {
            width: 60px;
            height: 1px;
            background: #d4af37;
            margin: 1.5rem auto;
            opacity: 0.5;
        }

        .blessing-text {
            font-size: 1.1rem;
            line-height: 1.8;
            color: #e0e0e0;
            font-style: italic;
        }

        /* Keyframe Animations */
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        @keyframes pulse {
            0%, 100% { transform: scale(1); opacity: 0.8; }
            50% { transform: scale(1.1); opacity: 1; }
        }

        /* Decorative Background Elements */
        .aura {
            position: absolute;
            width: 300px;
            height: 300px;
            background: rgba(212, 175, 55, 0.05);
            filter: blur(80px);
            border-radius: 50%;
            z-index: -1;
        }
    </style>
</head>
<body>

    <div class="aura"></div>

    <main class="card">
        <span class="om-symbol">🕉️</span>
        
        <h1>Maha Shivaratri</h1>
        <p class="mantra">Om Namah Shivaya</p>
        
        <div class="divider"></div>
        
        <div class="blessing-text">
            May the divine energy of Lord Shiva<br>
            enlighten your soul and guide you<br>
            towards eternal peace and joy.
            <br><br>
            <strong>Shubh Maha Shivaratri</strong>
        </div>
    </main>

</body>
</html>
